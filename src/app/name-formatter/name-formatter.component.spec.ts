/*
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NameFormatterComponent } from './name-formatter.component';

describe('NameFormatterComponent', () => {
  let component: NameFormatterComponent;
  let fixture: ComponentFixture<NameFormatterComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ NameFormatterComponent ]
    })
      .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(NameFormatterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should format the name with a middle initial', () => {
    const name = {
      givenName: 'Robert',
      middleName: 'Cecil',
      familyName: 'Martin'
    };
    expect(component.formatName(name)).toBe('Robert C. Martin');
  });
});
*/
